# LINK-IN 구현 기능 명세

**기준일** 2026-08-18 · **대상** `Link-In-온라인 시스템` (실제 실행되는 통합 앱)

> 이 문서는 **실제로 코드에 구현되어 동작하는 기능**만 정리한 것입니다.
> 기획했으나 미구현인 항목은 포함하지 않았고, 제약이 있는 기능은 그 제약을 함께 적었습니다.
> 기술 구성은 [`TECH_STACK.md`](TECH_STACK.md), 파일 구분은 [`CODE_MAP.md`](CODE_MAP.md) 참고.

---

## 0. 전체 구조

앱은 하단 탭(모바일) / 좌측 내비(데스크톱) 기준 **4개 화면**으로 구성됩니다.

| 탭 | 화면 ID | 핵심 기능 |
| --- | --- | --- |
| 오늘 | `tab-today` | 루틴 3슬롯, 알람, 카메라 인증, 아바타 |
| 기록 | `tab-history` | 잔디맵, 주간 통계, 아카이브, 한 달의 기록 |
| 상점 | `tab-shop` | 아이템 구매, 옷장, 럭키박스 |
| 학급 서버 | `tab-classroom` | 픽셀 교실, 알림장, 화상·음성, 푸시 |

---

## 1. 루틴 관리 (오늘 탭)

### 1-1. 하루 3슬롯 제한

하루에 등록 가능한 루틴은 **최대 3개**입니다. 슬롯 번호는 순서에 따라 1~3으로 자동 재부여되며,
루틴을 삭제하면 남은 항목의 번호가 다시 매겨집니다.

### 1-2. 날짜 이동과 3가지 모드

날짜 피커와 좌우 화살표로 다른 날짜를 조회할 수 있고, **KST 기준**으로 모드가 갈립니다.

| 모드 | 조건 | 동작 |
| --- | --- | --- |
| `past` | 오늘 이전 | **읽기 전용.** 완료 결과만 확인. 편집·삭제·인증 모두 비활성 |
| `today` | 오늘 | 모든 기능 활성 |
| `future` | 오늘 이후 | 계획 수립용. 편집·삭제는 되지만 **완료·인증은 불가** |

모드가 바뀌면 상단에 안내 배너가 뜹니다. 자정이 지나면 자동으로 오늘 날짜로 갱신됩니다
(`checkMidnightUpdate`).

### 1-3. 루틴 조작

| 기능 | 설명 | 제약 |
| --- | --- | --- |
| 빠른 추가 | 입력창 + `＋` 버튼 | 3개 초과 불가 |
| 내용 수정 | 카드의 텍스트 입력 직접 편집 | 과거 날짜 불가 |
| 삭제 | 휴지통 버튼 | 과거 날짜 불가 |
| **내일로 토스** | 완료 못 한 루틴을 내일로 이동 | 오늘·미완료만 가능. 내일 슬롯이 3개면 거부 |
| 완료 인증 | 체크 버튼 → 카메라 모달 | 오늘·미완료만 가능 |

토스 시 알람도 함께 해제되고, 오늘 목록에서 제거된 뒤 내일 목록 마지막 슬롯에 추가됩니다.

### 1-4. 커스텀 루틴 라이브러리 (반복 일정)

자주 쓰는 루틴을 저장해 두고 해당 요일에 추천받는 기능입니다.

| 반복 옵션 | 동작 |
| --- | --- |
| `none` 필요할 때 선택 | 추천 안 함, 라이브러리에만 보관 |
| `daily` 매일 | 매일 추천 |
| `weekdays` 평일 | 월~금 추천 |
| `weekends` 주말 | 토·일 추천 |
| `custom` 요일 지정 | 체크한 요일에만 추천 |

조건에 맞는 날짜를 열면 **이미 등록된 루틴을 제외하고** 추천 버튼이 뜨며, 누르면 바로 슬롯에 추가됩니다.
과거 날짜이거나 슬롯이 3개 다 찼으면 추천을 표시하지 않습니다.

---

## 2. 알람

### 2-1. 두 갈래 알림

같은 알람이 **두 경로**로 예약됩니다.

1. **브라우저 로컬 타이머** — `setTimeout`. 탭이 열려 있을 때 동작
2. **서버 푸시 예약** — `/api/push/schedule` → `push_jobs` 저장 → 서버가 발송. 탭이 닫혀 있어도 동작

로컬 알림은 Service Worker가 있으면 SW를 통해, 없으면 `new Notification`으로, 권한이 없으면
`alert`로 표시하는 3단 폴백 구조입니다.

### 2-2. 시간 입력 UI

12시간제(오전/오후 + 시 + 분) 셀렉트로 입력받아 내부적으로 24시간제(`HH:MM`)로 변환해 저장합니다
(`convert12To24` / `convert24To12`). 기본값은 오전 8시 30분입니다.

알람은 두 곳에서 설정할 수 있습니다.

- 빠른 추가 입력줄의 시계 버튼 (등록과 동시에 알람 지정)
- 각 루틴 카드의 시계 버튼 (등록 후 개별 지정)

### 2-3. 알람 문구

시간대에 따라 문구가 달라집니다.

- 오전: `침대랑 백년가약 맺음? 💍 [루틴명] 딱 하나만 하고 다시 눕자!`
- 오후: `오늘 하루 아무것도 안 했다고 자책 금지 🙅‍♂️ 지금 [루틴명] 바로 시작!`

루틴을 완료하면 알람이 자동 해제됩니다. 이미 지난 시각은 예약하지 않습니다.
서버 푸시 예약은 **최대 7일 뒤**까지만 허용됩니다.

---

## 3. 사진 인증 (카메라 + AI)

### 3-1. 1.5초 부메랑 촬영

체크 버튼을 누르면 전체화면 카메라 모달이 열립니다.

| 항목 | 값 |
| --- | --- |
| 촬영 길이 | 1.5초 |
| 프레임 간격 | 120ms (약 12~13프레임) |
| 저장 형식 | `canvas.toDataURL('image/jpeg')` |
| 워터마크 | 각 프레임 우하단에 `YYYY-MM-DD HH:MM:SS` (레트로 오렌지) |

촬영 중에는 뷰파인더가 흔들리고 플래시 효과와 원형 진행 게이지가 표시됩니다.
촬영이 끝나면 프레임을 **핑퐁 방식으로 왕복 재생**해 부메랑 효과를 냅니다.

전·후면 카메라 전환을 지원하며, 전면일 때는 좌우 반전(`scaleX(-1)`)을 적용합니다.
카메라 장치가 없으면 일러스트 플레이스홀더가 표시됩니다.

### 3-2. AI 판독

대표 프레임(가운데 프레임)을 Gemini에 보내 미션 내용과 사진이 일치하는지 판정받습니다.

| 판정 경로 | 조건 | 동작 |
| --- | --- | --- |
| **서버 프록시** | 서버에 `GEMINI_API_KEY` 있음 | 브라우저는 키를 모름. 로그인 필요 |
| BYOK | 서버 키 없음 + 사용자가 직접 키 입력 | 브라우저가 Gemini 직접 호출 |
| 데모 판정 | 키가 아예 없음 | 키워드 정규식 + 난수. `[데모 판정]` 라벨 표시 |

- **통과 기준: 70점.** 관련 물건(책·필기구·모니터·덤벨 등)이 보이면 고득점, 빈 방·어두운 화면은 저득점
- 피드백은 MZ 톤의 구어체로 생성 (성공 시 칭찬, 실패 시 팩폭)
- 판독 전 1.5초 연출 지연이 들어갑니다

### 3-3. 결과 화면 3종

| 결과 | 표시 | 선택지 |
| --- | --- | --- |
| 통과 | `PASS` 배지, 점수, 피드백 | 달력에 반영하기 |
| 실패 | `RETAKE` 배지, 점수, 피드백 | 다시 촬영 / 실패 확정 |
| **판독 오류** | `RETAKE` 배지, **점수 없음** | 다시 촬영만 |

판독 오류는 실패 판정이 **아닙니다.** 점수 배지와 '실패 확정' 버튼을 감추고
"성공·실패 어느 쪽으로도 기록되지 않습니다"를 명시합니다. 난수 보상을 주지 않기 위한 처리입니다.

### 3-4. 통과 시 처리

1. 체크박스에 **컨페티 파티클** 폭발 연출
2. 햅틱 진동 (`navigator.vibrate([80, 40, 120])`)
3. 루틴을 완료 처리하고 `verification_type: 'camera_ai'` 기록
4. 알람 해제
5. **2코인 지급** (중복 방지 — 같은 루틴은 1회만)
6. 부메랑 클립(최대 12프레임)을 '한 달의 기록'에 저장
7. 아바타·잔디·주간 통계 갱신
8. Supabase 동기화 (실패해도 로컬에는 저장, 상태만 `offline` 표시)

---

## 4. 캐릭터 성장 (아바타)

### 4-1. 4단계 성장

**당일 완료한 루틴 개수**가 곧 성장 단계입니다.

| 완료 | 단계 | 이미지 |
| --- | --- | --- |
| 0개 | Lv.0 잠자는 새싹 | `sprout_stage1` |
| 1개 | Lv.1 | `sprout_stage2` |
| 2개 | Lv.2 | `sprout_stage3` |
| 3개 | Lv.3 만개 | `sprout_stage4` |

### 4-2. 게으름 모드

**어제 완료가 0개이고 오늘도 아직 0개**이면 게으름 상태로 렌더링됩니다.
오늘 하나라도 완료하면 즉시 해제됩니다.

### 4-3. 렌더링

`avatar.js`가 SVG를 동적으로 생성하며, 장착한 스킨·액세서리·배경·칭호를 합성합니다.

---

## 5. 보상 경제 (상점 탭)

### 5-1. 코인

| 획득 | 금액 |
| --- | --- |
| 카메라 AI 인증 통과 | 2코인 (루틴당 1회) |
| 럭키박스 '일반' 등급 | 1코인 환급 |
| 럭키박스 전체 수집 완료 | 2코인 환급 |

루틴 완료를 취소하면 지급했던 보상이 회수됩니다 (`revokeLocalTaskReward`).

### 5-2. 아이템 카탈로그 (18종)

| 등급 | 개수 | 가격대 | 예시 |
| --- | --- | --- | --- |
| 희귀 💙 | 6종 | 1코인 | 아이스 아메리카노, 득근 아령, 에어팟 실버, 땀 밴드, 모던 안경, 클래식 오크 |
| 영웅 💜 | 6종 | 3~5코인 | 슈렉 그린, 에일리언 퍼플, 딸기 라떼, 퍼플 아령, 그린 에어팟, 딥 포레스트 |
| 전설 🌟 | 6종 | 5~15코인 | 황금 골드(15), 사이버 네온 블루(10), 무지개 에어팟(8), 갤럭시 라떼, 황금 아령, 빈티지 애쉬 |

카테고리는 **스킨 / 액세서리 / 배경 / 칭호** 4종으로 자동 분류되며(`getItemCategoryType`),
상점과 옷장 모두 탭으로 필터링됩니다. 탭 줄은 드래그와 휠 스크롤을 지원합니다.

### 5-3. 럭키박스 (가챠)

**비용 2코인.** 등급별 확률은 다음과 같습니다.

| 등급 | 확률 | 결과 |
| --- | --- | --- |
| 전설 🌟 | 10% | 전설 아이템 1개 |
| 영웅 💜 | 25% | 영웅 아이템 1개 |
| 희귀 💙 | 40% | 희귀 아이템 1개 |
| 일반 ⚪ | 25% | **1코인 환급** (실질 비용 1코인) |

**중복 방지 로직**이 들어 있습니다. 뽑힌 등급의 아이템을 이미 다 가지고 있으면
다른 등급의 미보유 아이템 중에서 지급하고, 모든 아이템을 보유했으면
'럭키박스 마스터' 판정과 함께 2코인을 전액 환급합니다.

**럭키박스 한정 아이템 2종**은 상점에서 살 수 없고 가챠로만 얻습니다.

- 오로라 왕관 👑
- 레트로 선글라스 🥽

당첨 시 3D 회전 애니메이션이 재생되고, 확률표는 도움말 모달에서 확인할 수 있습니다.

### 5-4. 칭호 (6종)

| 칭호 | 카탈로그상 설명 |
| --- | --- |
| 초보 도전자 | 첫 발을 내딛은 뉴비 (기본 장착값) |
| 작심삼일 브레이커 | 루틴 스트릭 3일 달성 |
| 새벽의 지배자 | 새벽 시간에 미션을 완료한 자 |
| 코인 부자 | 지갑에 코인이 두둑한 자산가 |
| 끈기의 화신 | 루틴 스트릭 7일 달성 |
| 범접불가 레전드 | 하루 루틴 3개를 올클리어한 자 |

칭호는 옷장에서 장착하면 아바타 옆에 표시됩니다. 신규 사용자는 `초보 도전자`가 기본 장착됩니다.

> ⚠️ **획득 조건은 아직 코드로 구현되어 있지 않습니다.** 위 설명(스트릭 3일, 7일, 올클리어 등)은
> 카탈로그의 문구일 뿐이고, 조건을 달성했을 때 칭호를 자동 지급하는 로직이 없습니다.
> 현재는 `user_items`에 들어 있는 칭호만 장착할 수 있습니다.

### 5-5. 옷장

보유 아이템을 장착·해제합니다. 카테고리별로 하나씩 장착되며 아바타에 즉시 반영됩니다.

---

## 6. 기록·통계 (기록 탭)

### 6-1. 잔디맵

**최근 35일**을 7×5 격자로 표시합니다. 칸 색은 그날 완료한 루틴 개수(0~3)에 따라 4단계입니다.

| 레벨 | 툴팁 |
| --- | --- |
| 0 | 쉬어갔어요 💤 |
| 1 | 1개 완료 🌱 |
| 2 | 2개 완료 🌿 |
| 3 | 3개 완료, 대성공! 🌸 |

각 칸에 `aria-label`과 `role="img"`을 부여해 스크린리더로도 읽힙니다.

### 6-2. 주간 통계

요일별 달성 개수를 막대 차트로 표시하며, 진입 시 막대가 차오르는 애니메이션이 재생됩니다.
막대 위에 미니 아바타 인디케이터가 붙습니다.

### 6-3. 아카이브

과거 기록을 목록으로 훑어볼 수 있습니다.

### 6-4. 한 달의 기록 (영상 릴)

카메라 인증으로 모은 부메랑 클립들을 **연속 재생**하는 기능입니다.

- 캔버스 360×260, 클립당 최대 16프레임 왕복 재생
- 재생/일시정지, 다시보기 지원
- 클립마다 루틴명·날짜·시각 표시
- 클립이 없으면 플레이스홀더 프레임을 그려 보여줍니다

---

## 7. 계정과 데이터 동기화

### 7-1. 3가지 사용 모드

| 모드 | 저장 위치 | 설명 |
| --- | --- | --- |
| 게스트 | `localStorage` (`guest` 스코프) | 가입 없이 즉시 사용 |
| 로컬 계정 | `localStorage` (계정별 스코프) | 이메일·비밀번호(6자 이상)로 이 기기에만 저장 |
| Supabase 계정 | 클라우드 + 로컬 | 기기 간 동기화 |

저장 키가 스코프별로 분리되어 있어(`scopedStorageKey`) 계정을 바꿔도 데이터가 섞이지 않습니다.

### 7-2. 게스트 데이터 이전

게스트로 쓰던 데이터가 있는 상태에서 Supabase 계정에 로그인하면
**'게스트 데이터 가져오기'** 버튼이 나타납니다. `date + slot_number + content` 조합으로
중복을 걸러 이미 있는 항목은 건너뜁니다. 한 번 이전하면 버튼이 사라집니다.

### 7-3. 계정 버튼과 로그아웃

화면 상단 우측에 계정 버튼이 있습니다. 로그인 상태면 계정명이 강조색으로, 게스트면 '게스트'로 표시됩니다.
누르면 계정 모달이 열립니다.

로그아웃을 누르면 **확인 모달**이 먼저 뜹니다.

| 안내 항목 | 조건 |
| --- | --- |
| 계정 이메일 | 항상 |
| 게스트 모드로 전환됨 | 항상 |
| 클라우드 기록은 삭제되지 않음 | 항상 |
| **학급 서버 연결도 끊김** | 학급 접속 중일 때만 |

배경 클릭과 ESC로 취소할 수 있고, 처리 중에는 버튼이 비활성화됩니다.
로그아웃이 완료되면 Supabase 세션 종료 → 게스트 스코프 전환 → `lockin-auth-changed` 이벤트가 발행되고,
`online.js`가 이를 받아 학급 소켓·WebRTC·픽셀 교실을 정리합니다.

### 7-4. 동기화 상태 표시

헤더에 현재 상태를 실시간 표시합니다.

`로컬 계정 · 이메일` / `게스트 모드 · 이 기기에 저장됨` / `클라우드 연결됨` /
`클라우드에 저장 중…` / `클라우드 백업 완료` / `오프라인 · 이 기기에 안전하게 저장됨`

**오프라인 우선 설계**입니다. 클라우드 저장에 실패해도 로컬에는 반드시 기록되고
상태만 `offline`으로 바뀝니다.

---

## 8. 학급 서버 (학급 탭)

### 8-1. 학급 입장

학교명·학년·반·표시이름을 입력하고 **캐릭터를 고른 뒤** 입장합니다.

- **LOCK-IN 계정 로그인이 선행되어야 합니다.** 미로그인 시 안내 후 계정 모달이 열립니다
- `join_class` RPC로 학급을 생성하거나 가입합니다 (없으면 자동 생성)
- **학급 변경은 30일에 1회**로 제한됩니다 (DB 함수에서 강제)

### 8-1-1. 역할 선택 (학생 / 선생님)

가입 시 역할을 고릅니다. **한 번 정해지면 이후 변경할 수 없습니다.**

| 역할 | 선택 방법 | 권한 |
| --- | --- | --- |
| 🎒 학생 | 기본값, 바로 선택 | 입장, 이동, 알림장 읽기 |
| 👩‍🏫 선생님 | **교사 코드 입력 필요** | + 알림장 작성, 반장 임명 |
| 👑 반장 | **선택 불가.** 선생님이 학급에서 임명 | + 알림장 작성 |

선생님을 고르면 교사 코드 입력란이 나타납니다. 코드는 `app_config` 테이블에 저장되고
`security definer` 함수 안에서만 대조되므로, 브라우저 콘솔로 RPC를 직접 호출해도 우회할 수 없습니다.

이미 가입한 사용자에게는 선택 버튼 대신 **잠긴 배지**(`🎒 학생 🔒`)가 표시됩니다.
학급을 옮겨도 역할은 유지됩니다. 단 반장이 이미 반장이 있는 학급으로 옮기면 학생이 됩니다
(한 반에 반장은 한 명이라는 DB 제약 때문입니다).

> 교사 코드는 관리자가 Supabase에서 한 번 등록합니다. 자세한 절차는
> `supabase/SETUP_TEACHER_CODE.sql` 참고.

### 8-1-2. 캐릭터 6종 선택

입장 폼에서 새싹 캐릭터 6종 중 하나를 고릅니다.

| ID | 이름 | 특징 | 대표색 |
| --- | --- | --- | --- |
| `red` | 빨강이 | 승부욕 만렙 (주먹 쥔 포즈) | `#D2544F` |
| `orange` | 주황이 | 엄지척 응원러 (물음표 무늬) | `#E4823C` |
| `yellow` | 노랑이 | 해맑은 햇살 (활짝 웃음) | `#EFC33F` |
| `green` | 초록이 | 차분한 모범생 (옷깃) | `#4E7346` |
| `blue` | 파랑이 | 안경 쓴 지식인 | `#2F80B4` |
| `purple` | 보라 | 망토 두른 시크 | `#9187AE` |

- 선택하지 않은 캐릭터는 흑백으로 흐리게, 고른 캐릭터는 원색과 테두리로 강조됩니다
- 좌우 방향키로도 고를 수 있습니다 (`role="radiogroup"` 접근성 처리)
- **서버가 6개 ID 화이트리스트로 검증**합니다. 목록 밖의 값을 보내면 기본값으로 대체됩니다
- 선택한 캐릭터는 **픽셀 교실·구성원 목록·이름표 테두리 색**에 일괄 반영됩니다
- 이미지 로딩 전이거나 실패하면 기존 도형 캐릭터로 자동 폴백합니다

### 8-2. 서버 주도 권한 판정

**클라이언트가 보낸 역할을 신뢰하지 않습니다.** 서버가 Socket.IO 핸드셰이크에서
Supabase 세션 토큰을 검증하고, 학급과 역할을 DB(`class_memberships`)에서 직접 조회해 결정합니다.

| 역할 | 권한 |
| --- | --- |
| `student` 학생 | 입장, 이동, 알림장 읽기 |
| `president` 반장 | + 알림장 작성·수정 |
| `teacher` 선생님 | + 알림장 작성·수정, 반장 임명 |

반장은 학급당 1명만 존재하도록 DB 인덱스로 보장됩니다.

### 8-3. 픽셀 교실

2D 캔버스로 그린 교실에서 아바타를 움직입니다.

| 항목 | 내용 |
| --- | --- |
| 조작 | 방향키 / `WASD` / 화면 D-pad 버튼 |
| 이동 속도 | 3.5px per frame |
| 방향 | 상·하·좌·우 4방향. 왼쪽 이동 시 스프라이트 좌우 반전 |
| 걷기 연출 | 다리 스텝 대신 위아래로 통통 튀는 hop 애니메이션 |
| 역할 표시 | 이름표의 아이콘(👩‍🏫 / 👑 / 🎒)으로 구분 |
| 맵 요소 | 바닥 타일, 앞쪽 벽, 칠판, 교탁, 태극기, 책상 12개 |
| 동기화 | 좌표를 소켓으로 브로드캐스트 |

입력창(`INPUT`/`TEXTAREA`)에 포커스가 있을 때는 키 입력을 무시해 채팅 중 캐릭터가
움직이지 않도록 처리했습니다. 서버는 좌표를 0~1200 / 0~800으로 clamp하고
방향값도 화이트리스트로 검증합니다.

### 8-4. 구성원 목록

접속 중인 학생을 카드 그리드로 표시하고 인원수를 함께 보여줍니다.
입장·퇴장 시 실시간 갱신됩니다.

### 8-5. 알림장

| 기능 | 설명 |
| --- | --- |
| 열람 | 전원 가능. 작성자 이름·역할·수정 시각 표시 |
| 작성 | **선생님·반장만.** 서버에서 역할 재확인 |
| 저장 | Supabase `class_notices`에 영속 저장 |
| 브로드캐스트 | 저장 즉시 같은 반 전원에게 실시간 전송 |
| 새 글 표시 | 헤더 버튼에 배지 점 표시 |

제목 100자, 내용 5000자로 제한됩니다. 처음 입장한 반에는 기본 안내문이 표시됩니다.

### 8-6. 화상·음성 (WebRTC)

같은 학급끼리 P2P mesh로 연결됩니다.

- 카메라 + 마이크 시도 → **장치가 없으면 자동으로 오디오 전용 재시도**
- 오디오 전용일 때는 버튼과 상태 문구가 '마이크 모드'로 바뀝니다
- 시그널링(offer/answer/ICE)은 서버가 릴레이하되 **같은 룸인지 확인 후에만** 전달
- ICE 서버는 Google STUN 하나 (⚠️ TURN 없음 — 엄격한 방화벽 뒤에서는 실패 가능)

### 8-7. 백그라운드 푸시

'백그라운드 알림 켜기' 버튼으로 브라우저 푸시를 구독합니다.
구독 시 대기 중이던 예약이 자동 복원되고 `백그라운드 알림 N건을 복원했습니다` 문구가 표시됩니다.

### 8-8. 로그아웃 연동

다른 탭에서 계정을 로그아웃하면 `lockin-auth-changed` 이벤트를 받아
소켓 연결·픽셀 교실·WebRTC 미디어·피어를 모두 정리하고 학급 로그인 화면으로 복귀합니다.

---

## 9. PWA

| 기능 | 구현 |
| --- | --- |
| 홈 화면 추가 | `manifest.json`, 192/512 아이콘, iOS 메타 태그 |
| 오프라인 캐시 | Service Worker cache-first (앱 셸 + 자산 20종) |
| 푸시 수신 | SW `push` 이벤트 → 알림 표시 |
| 알림 클릭 | 앱으로 이동 |
| 항상 최신 유지 | 내비게이션 요청과 `/runtime-config.js`는 네트워크 우선 |

캐시 버전은 현재 `link-in-integrated-v13`입니다.

---

## 10. 반응형 UI

| 화면 폭 | 레이아웃 |
| --- | --- |
| < 900px | 단일 열, 하단 탭바 |
| ≥ 900px | 어두운 좌측 내비 + 최대 1280px 대시보드 |

데스크톱에서는 오늘 화면이 아바타/루틴 2열, 기록이 카드 2열, 상점이 폭에 따라 3~4열로 배치됩니다.
학급 서버는 가용 폭 전체를 사용합니다.

탭 전환 시 인디케이터 알약이 부드럽게 이동하며, 학급 탭의 다크 테마는 해당 탭과 모달 범위로만
한정되어 밝은 루틴 화면과 충돌하지 않습니다.

---

## 11. 보안·안전장치

| 항목 | 구현 |
| --- | --- |
| 역할 위조 방지 | 서버가 DB에서 역할 조회. 클라이언트 입력 불신 |
| 세션 검증 | Socket.IO 핸드셰이크 + 모든 API에서 Supabase 토큰 검증 |
| XSS | 사용자 문자열은 `escapeHtml` 또는 안전한 DOM 방식으로 처리 |
| RLS | 모든 신규 테이블에 Row Level Security 적용 |
| RPC 잠금 | `security definer` + `search_path = ''`, `anon` 실행 권한 회수 |
| 좌표 검증 | 서버에서 범위 clamp |
| 알림장 권한 | 소켓 핸들러와 DB RPC **양쪽**에서 검증 |
| AI 키 보호 | 서버 프록시. 브라우저는 키를 받지 못함 |
| AI 남용 방지 | 로그인 필수 + 5회/분·40회/시간 + 이미지 2MB 상한 |
| 비밀값 유출 가드 | 기동 직전 `/runtime-config.js`에 비밀값이 섞였는지 검사, 발견 시 서버 미기동 |
| 보상 무결성 | 판독 실패 시 난수 보상 지급 안 함, 중복 지급 방지 |

---

## 12. 알려진 제약

| 기능 | 제약 |
| --- | --- |
| 화상 통화 | TURN 없음 → 엄격한 NAT/방화벽에서 실패 가능. mesh라 대규모 학급 부적합 |
| 푸시 예약 | 단일 프로세스 `setTimeout`. 서버 재시작 시 유예 10분 초과분 유실 |
| 학급 룸 | 프로세스 메모리 → 서버 여러 대로 늘리면 같은 반이 분리됨 |
| AI 호출 한도 | 프로세스 메모리 → 재시작 시 초기화 |
| 주간 통계 | `localStorage` 기반이라 기기 간 동기화되지 않음 |
| 칭호 | 카탈로그와 장착 기능만 있고 **자동 획득 조건이 미구현** |
| 데모 판정 | 키가 없으면 난수 판정으로 코인이 지급됨 (`[데모 판정]` 라벨로 구분) |
| 오프라인 최초 실행 | CDN 4곳(Supabase·Socket.IO·Lucide·Google Fonts)에 의존 |

세부 내용과 후속 후보는 [`AUDIT.md`](AUDIT.md)를 참고하세요.
