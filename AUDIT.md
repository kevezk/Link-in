# LINK-IN 전체 재검사 결과

## 현재 기준 앱

- 실행·개발 기준은 `Link-In-온라인 시스템`의 Express/Socket.IO 앱과 `public` 자산이다.
- 최상위 `index.html`, `Link-In-알림 기능 추가 버전/complete`, `login/src`, `Link-In-온라인 시스템/src`는 전달받은 원본 또는 미연결 샘플로 보존한다.
- 기존 파일을 삭제하거나 덮어쓰지 않고 기준 앱에 기능을 통합했다.

## 구현 및 재검증 완료

- Supabase 세션을 검증한 사용자만 Socket.IO 학급에 입장한다.
- 학급과 역할은 서버가 DB 멤버십을 조회해 결정하며 클라이언트 입력 역할을 신뢰하지 않는다.
- 입장 값과 이동 좌표를 서버에서 검증하고, 사용자 표시 문자열은 안전한 DOM 방식으로 처리한다.
- 학급 변경 제한과 학급 멤버십은 Supabase에 저장한다.
- 학급 알림장은 Supabase에 영속 저장하며 권한이 있는 사용자만 작성한다.
- LOCK-IN 자산, Pixel Classroom, WebRTC mesh 시그널링이 기준 앱에서 로드된다.
- Push 구독과 예약 작업은 Supabase에 저장되고 Node 서버가 예약 전송한다.
- 로그아웃 시 실시간 연결도 해제된다.
- 두 계정 로컬 E2E에서 멤버십 동기화, 역할, 알림장 저장, Pixel 이동, 로그아웃 연결 해제를 확인했다.

## 남은 제한 및 후속 후보

1. 카메라는 테스트 PC에 장치가 없어 환경 제한이다. Edge 실검증에서 카메라 없는 환경의 마이크 전용 연결은 통과했다. 실제 Windows Push 알림 검증은 브라우저 자동화 권한 문제 이후 사용자 요청에 따라 제외했다.
2. WebRTC는 STUN만 설정되어 있어 제한적인 기업·학교 네트워크에서는 TURN 서버가 필요할 수 있다.
3. Push 예약 복원은 2026-08-18에 서버 부팅 sweep과 주기적 재점검으로 바꿨다. **다만 이를 켜는 `SUPABASE_SERVICE_ROLE_KEY`는 의도적으로 설정하지 않기로 결정했다.** 이 키는 RLS를 전면 우회하므로 유출 시 전체 사용자 데이터가 노출되는데, 얻는 이득이 "서버 재시작 + 해당 사용자가 알람 시각 전까지 미접속"이라는 좁은 경우에 한정되기 때문이다. 사용자가 앱을 열면 `online.js`의 `restoreExistingPushSubscription()`이 대기 예약을 복원하므로 일반적인 사용에서는 공백이 없다. 상시 백그라운드 발송이 실제로 필요해지면 키를 서버에 두는 대신 Supabase Edge Function + cron으로 옮기는 편이 안전하다. sweep 코드는 opt-in이라 키가 없으면 경고 로그만 남기고 비활성 상태로 유지된다. 여전히 단일 Node 프로세스의 타이머이므로 서버가 내려가 있던 동안의 예약은 유예 시간(기본 10분) 밖이면 `failed`로 기록된다. 프로세스와 무관한 상시 실행이 필요하면 별도 워커나 cron 설계가 필요하다.
4. 별도 카테고리 가챠 샘플은 원본으로 남아 있지만, 기준 앱의 럭키박스는 실제 아이템 카탈로그·`user_items`·옷장 장착 흐름에 이미 연결되어 있다.
5. Gemini 호출 실패 시 mock 결과로 보상을 주던 동작은 2026-08-18에 제거했다. 이제 판독 실패는 판정 없이 오류로 표시하고 재촬영만 허용한다. API 키가 없는 사용자의 데모 판정은 유지하되 `[데모 판정]` 표시를 붙인다.
6. 원본에 포함된 중복·미연결 샘플은 추적 가능성을 위해 그대로 보존 중이다. 어떤 파일이 활성 코드이고 어떤 파일이 보존본인지는 `CODE_MAP.md`에 해시와 함께 정리했다.
7. Gemini 판독은 2026-08-18에 서버 프록시(`POST /api/ai/analyze`)로 전환했다. 키는 서버 `.env`에만 있고 클라이언트에는 `AI_PROXY_ENABLED` 플래그만 내려간다. 로그인 검증, 사용자별 호출 한도, 이미지 크기 상한을 적용했다. 실기기 검증에서 신형 `AQ.` 키와 `x-goog-api-key` 헤더 방식이 정상 동작함을 확인했고, `gemini-2.5-flash`가 신규 사용자 차단으로 404를 반환하는 것을 발견해 `gemini-3.6-flash`로 교체했다. 남은 확인 항목은 실제 사진으로 판정이 통과하는지다. 호출 한도는 단일 프로세스 메모리 기준이라 재시작 시 초기화되고 수평 확장 시 인스턴스별로 집계된다.
8. 학급 룸 상태(`roomsData`)는 단일 프로세스 메모리에만 있어 수평 확장 시 룸이 분리된다. (미해결)
9. 가입 시 역할(학생/선생님) 선택을 2026-08-18에 추가했다. 선생님은 교사 코드로 검증하며, 코드는 `app_config` 테이블에 두고 `security definer` 함수 안에서만 대조한다. 브라우저가 RPC를 직접 호출할 수 있으므로 클라이언트·Node 서버 검사는 우회 가능해 DB 내부 검증이 유일한 방어선이다. **마이그레이션 `20260818093000`은 아직 원격에 적용되지 않았다.** 적용 전까지 학급 가입이 동작하지 않는다.
10. 선생님의 학급 추방 기능은 요청되었으나 아직 구현하지 않았다. 소켓 강제 종료만으로는 즉시 재입장이 가능하므로, 재입장 차단 방식(메모리 쿨다운 vs DB 밴 목록)을 먼저 정해야 한다. (미구현)

## Supabase 안전성 확인

- 원격 기존 `profiles`를 재생성하는 레거시 `schema.sql`은 실행하지 않았다.
- 신규 스키마는 migration SQL로 관리하고 Supabase MCP로 적용했다.
- RPC의 `search_path`와 실행 권한, 테이블 RLS 및 익명 접근 권한을 보강했다.
- 요청 범위를 벗어나 추가했던 Edge Push dispatch 객체는 승인 후 rollback migration을 Supabase MCP로 적용해 제거했다.

## 검사 범위

- `npm run check`는 서버와 기준 앱 JavaScript 전체(이제 `pixelClassroom.js` 포함)를 문법 검사한다.
- 로컬 서버의 `/`와 주요 정적 자산 HTTP 응답을 확인했다.
- 통합 HTML의 필수 DOM ID와 중복 ID 여부를 확인했다.
