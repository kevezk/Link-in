# LINK-IN 기술 스택 — 쉽게 풀어쓴 설명

**기준일** 2026-08-18 · **대상** `Link-In-온라인 시스템` (실제 실행되는 통합 앱)

> **이 문서를 읽는 법**
> 1~9장은 개발을 모르는 분도 읽을 수 있도록 비유를 섞어 풀어 썼습니다.
> 정확한 버전 번호와 표는 **10장 부록**에 모아 뒀으니, 개발자는 거기부터 보셔도 됩니다.
>
> 관련 문서: 기능 목록은 [`FEATURES.md`](FEATURES.md), 파일 구분은 [`CODE_MAP.md`](CODE_MAP.md)

---

## 1. 세 줄 요약

- LINK-IN은 **웹사이트인데 앱처럼 쓸 수 있는 프로그램**입니다. 앱스토어에서 받지 않고 주소만 열면 됩니다.
- **재료를 최소한으로 썼습니다.** 남이 만든 부품을 3개만 가져다 쓰고 나머지는 직접 만들었습니다.
- **"조립 과정"이 없습니다.** 보통 웹 개발은 코드를 기계용으로 번역하는 단계를 거치는데,
  이 프로젝트는 그 단계를 통째로 생략했습니다. 이게 가장 큰 특징입니다.

---

## 2. 전체 그림 — 세 개의 장소

프로그램은 **서로 다른 세 곳**에서 나눠 돌아갑니다. 음식점에 비유하면 이렇습니다.

| 장소 | 비유 | 실제로 하는 일 |
| --- | --- | --- |
| **사용자의 브라우저** | 손님이 앉은 홀 | 화면을 그리고, 버튼 클릭을 받고, 카메라로 촬영 |
| **서버 (Node.js)** | 주방 | 주문을 받아 처리하고, 학급 채널을 중계하고, AI에 판독 의뢰 |
| **Supabase (클라우드)** | 창고 겸 금고 | 회원 정보와 모든 기록을 안전하게 보관 |

여기에 **외부 업체 두 곳**에 일부 일을 맡깁니다.

- **Google Gemini** — 사진을 보고 "이게 정말 공부하는 사진 맞아?"를 판정하는 AI
- **브라우저 제조사의 푸시 서버** — 앱이 꺼져 있어도 알림을 배달해 주는 우체국

```
[브라우저]  ←→  [서버]  ←→  [Supabase 창고]
     │             │
     │             └──→ [Gemini AI]  (사진 판정)
     │
     └──────────────────→ [푸시 서버]  (알림 배달)
```

---

## 3. 사용자의 화면 — 프런트엔드

### 3-1. 웹사이트인데 앱처럼 (PWA)

**PWA**는 "웹사이트를 앱처럼 쓰게 해 주는 기술"입니다. 브라우저에서 열지만,

- 홈 화면에 아이콘으로 추가할 수 있고
- 주소창 없이 전체화면으로 뜨고
- 인터넷이 끊겨도 기본 화면은 열리고
- 앱을 닫아도 알림이 옵니다

앱스토어 심사나 설치 과정이 없다는 게 가장 큰 장점입니다. 학생들에게 "이 주소 들어가서
홈 화면에 추가하세요"로 배포가 끝납니다.

이걸 가능하게 하는 부품이 **서비스 워커(Service Worker)** 입니다.
브라우저 뒤편에서 조용히 도는 작은 프로그램인데, 이런 일을 합니다.

- 화면 구성 파일들을 미리 저장해 둠 → 인터넷이 없어도 앱이 열림
- 앱이 꺼져 있을 때 도착한 알림을 받아서 띄워 줌

> **주의할 점 하나.** 서비스 워커는 "저장해 둔 것부터 보여주는" 성격이라,
> 코드를 고쳐도 사용자 화면에는 옛날 것이 계속 보일 수 있습니다.
> 그래서 파일을 수정할 때마다 **"버전 번호를 손으로 올려주는"** 작업이 반드시 필요합니다.
> 이걸 잊으면 "분명 고쳤는데 왜 그대로지?" 상황이 생깁니다.

### 3-2. 가장 특이한 선택 — 조립 과정이 없다

이 프로젝트를 이해하는 데 가장 중요한 부분입니다.

보통 요즘 웹 개발은 이런 과정을 거칩니다.

```
개발자가 쓴 코드 → [번역·압축·합치기] → 브라우저가 읽는 코드
                      (빌드 과정)
```

이 중간 단계를 **"빌드"** 라고 하고, 여기에 쓰이는 도구가 webpack, Vite 같은 것들입니다.
왜 필요하냐면, 개발자가 쓰기 편한 문법과 브라우저가 이해하는 문법이 다르기 때문입니다.

**LINK-IN에는 이 중간 단계가 없습니다.** 개발자가 쓴 코드를 브라우저가 그대로 읽습니다.

| | 빌드가 있는 방식 | LINK-IN (빌드 없음) |
| --- | --- | --- |
| 배포 | 빌드 명령 실행 후 결과물 업로드 | 파일 그대로 올리면 끝 |
| 수정 확인 | 빌드 기다림 | 새로고침하면 즉시 |
| 도구 설치 | 수십~수백 개 | 3개 |
| 오타·실수 잡기 | 빌드가 미리 잡아줌 | **못 잡음. 실행해 봐야 앎** |
| 파일 크기 최적화 | 자동 | 없음 |

**장점은 단순함**입니다. 처음 보는 사람도 파일을 열면 바로 무슨 코드인지 읽을 수 있고,
"이거 고치려면 뭘 설치해야 하지?" 같은 진입 장벽이 없습니다.

**단점은 안전망이 없다는 것**입니다. 변수 이름을 잘못 쓰거나 함수를 잘못 호출해도
미리 알려주는 사람이 없어서, 직접 실행해 보기 전까지 모릅니다.
그래서 이 프로젝트에서는 `npm run check`라는 **최소한의 문법 검사**가
사실상 유일한 자동 점검 수단입니다. (문법이 맞는지만 보고, 논리가 맞는지는 못 봅니다.)

### 3-3. 화면을 그리는 두 가지 방식

LINK-IN은 상황에 따라 두 방식을 섞어 씁니다.

**① 일반 웹 요소 (HTML/CSS)** — 버튼, 입력창, 목록 등
문서를 만들듯 요소를 배치하는 방식입니다. 루틴 카드, 상점 목록, 알림장 등이 여기 해당합니다.

**② 캔버스 (Canvas)** — 그림판처럼 자유롭게 그리는 영역
"좌표를 찍어서 직접 그리는" 방식이라 자유도가 높습니다. 이런 곳에 씁니다.

- 4단계로 자라는 새싹 캐릭터와 장착 아이템
- 픽셀 교실의 교실 바닥·벽·책상과 움직이는 캐릭터
- 카메라로 찍은 1.5초 부메랑 프레임 저장
- AI에 보내기 전 사진 크기 줄이기

캐릭터가 걷고 아이템이 겹쳐 보이는 건 게임 엔진을 쓴 게 아니라
**전부 좌표 계산으로 직접 그린 것**입니다.

### 3-4. 디자인

Bootstrap이나 Tailwind 같은 **디자인 틀을 쓰지 않고 CSS를 직접 작성**했습니다.
색상은 한 곳에 모아 관리(CSS 변수)하고, 화면 너비 900px을 기준으로

- **좁은 화면(휴대폰)**: 아래쪽에 탭바, 한 줄 배치
- **넓은 화면(PC)**: 왼쪽에 어두운 메뉴, 여러 열 배치

로 자동 전환됩니다.

### 3-5. 코드 파일 구성

프런트엔드는 역할별로 6개 파일로 나뉩니다.

| 파일 | 담당 |
| --- | --- |
| `app.js` | 루틴, 알람, 카메라, 가챠, 통계 — **가장 큰 파일 (약 3,000줄)** |
| `store.js` | 데이터 저장·불러오기 (내 기기 ↔ 클라우드) |
| `avatar.js` | 캐릭터와 아이템 그리기 |
| `ai.js` | 사진 판정 요청 |
| `online.js` | 학급 서버 접속, 알림장, 화상통화, 푸시 |
| `pixelClassroom.js` | 픽셀 교실 맵과 캐릭터 이동 |

> `app.js` 하나가 3,000줄로 지나치게 큽니다. 기능이 더 늘어나면 나누는 게 좋습니다.

### 3-6. 남의 코드를 인터넷에서 빌려 쓰는 부분

설치하지 않고 **인터넷 주소로 직접 불러오는** 부품이 4개 있습니다.

| 부품 | 하는 일 |
| --- | --- |
| Supabase 라이브러리 | 로그인과 데이터 저장을 쉽게 해 주는 도구 |
| Socket.IO 라이브러리 | 실시간 통신 도구 |
| Lucide | 아이콘 모음 |
| Google Fonts | 글꼴 3종 |

> **두 가지 주의점이 있습니다.**
>
> **첫째,** 이 4곳 중 하나라도 접속이 안 되면 화면이 제대로 안 뜹니다.
> 완전한 오프라인 사용은 불가능합니다.
>
> **둘째,** 아이콘(Lucide)만 **"항상 최신 버전"으로 설정**되어 있습니다.
> 만든 쪽에서 뭔가 바꾸면 우리가 아무것도 안 했는데 아이콘이 깨질 수 있습니다.
> 버전을 고정하는 게 안전합니다.

---

## 4. 서버 — 백엔드

### 4-1. Node.js — 서버에서 도는 자바스크립트

원래 자바스크립트는 웹 브라우저 안에서만 도는 언어였습니다.
**Node.js**는 이걸 브라우저 밖(서버)에서도 돌릴 수 있게 해 주는 프로그램입니다.

덕분에 이 프로젝트는 **화면과 서버를 같은 언어로 작성**합니다.
개발자가 언어를 두 개 배우지 않아도 되고, 코드를 서로 옮겨 쓰기도 쉽습니다.

현재 **Node.js 22 버전**을 쓰는데, 이 버전이 기본으로 제공하는 기능들 덕에
따로 설치할 부품을 많이 줄일 수 있었습니다.

- 비밀번호·API 키를 파일에서 읽어오는 기능 (예전엔 `dotenv` 설치 필요)
- 다른 서버에 요청 보내는 기능 (예전엔 `axios` 설치 필요)
- 응답이 너무 오래 걸리면 포기하는 기능

### 4-2. Express — 손님 응대 담당

**Express**는 "누가 어떤 주소로 들어오면 무엇을 보여줄지" 정하는 도구입니다.
호텔 프런트 데스크라고 생각하면 됩니다.

LINK-IN 서버가 응대하는 창구는 4개입니다.

| 창구 | 하는 일 | 로그인 필요? |
| --- | --- | --- |
| 접속 설정 안내 | 앱에 필요한 공개 설정값을 알려줌 | ❌ |
| 알림 구독 등록 | "이 사람에게 알림 보내도 됩니다" 등록 | ✅ |
| 알림 예약 | "내일 아침 8시에 알려줘" 접수 | ✅ |
| **AI 사진 판독** | 사진을 받아 Gemini에 대신 물어봄 | ✅ |

### 4-3. Socket.IO — 실시간 통화선

일반적인 웹은 **편지**처럼 동작합니다. 사용자가 요청을 보내면 서버가 답장하고 끝.
서버가 먼저 말을 걸 수는 없습니다.

그런데 학급 교실에서는 **친구가 움직이면 내 화면에서도 즉시 움직여야** 합니다.
편지 방식으로는 "혹시 누가 움직였나요?"를 1초에 수십 번 물어봐야 해서 비효율적입니다.

**Socket.IO**는 이걸 **전화 통화**처럼 바꿔 줍니다.
한 번 연결해 두면 양쪽이 아무 때나 말을 걸 수 있습니다.

이걸로 처리하는 것들:

- 학급 입장·퇴장 알림
- 픽셀 교실에서 캐릭터 위치 공유
- 알림장이 수정되면 같은 반 전원 화면에 즉시 반영
- 화상통화 연결을 위한 사전 연락 주고받기

**학급별로 방(room)이 나뉘어** 있어서, 1반 학생의 움직임이 2반에 보이지 않습니다.

### 4-4. 부품을 3개만 쓴 이유

서버가 설치해서 쓰는 남의 코드는 **딱 3개**입니다.

| 부품 | 하는 일 |
| --- | --- |
| Express | 창구 응대 |
| Socket.IO | 실시간 통신 |
| web-push | 알림 발송 시 신원 증명 서명 |

요즘 프로젝트가 보통 수십 개를 쓰는 것에 비하면 매우 적습니다. 이게 좋은 이유는

- **보안 사고 위험이 낮습니다.** 남의 코드에서 취약점이 발견되는 일이 흔한데, 쓰는 게 적으면 그만큼 노출이 적습니다
- **버전 관리가 쉽습니다.** 업데이트할 게 3개뿐입니다
- **동작을 파악하기 쉽습니다.** "이건 어느 부품이 하는 거지?"를 헤맬 일이 적습니다

---

## 5. 데이터 보관 — Supabase

### 5-1. Supabase가 뭔가요

**서버 없이도 데이터베이스와 로그인 기능을 쓸 수 있게 해 주는 클라우드 서비스**입니다.
직접 구축하려면 며칠 걸릴 일을 몇 분 만에 끝낼 수 있습니다.

LINK-IN은 여기서 세 가지를 씁니다.

| 기능 | 설명 |
| --- | --- |
| **회원 관리** | 가입·로그인·비밀번호 처리를 대신해 줌 |
| **데이터베이스** | 루틴 기록, 학급 정보, 아이템 등 모든 데이터 보관 |
| **접근 권한 통제** | 누가 어떤 데이터를 볼 수 있는지 자동으로 걸러 줌 |

### 5-2. RLS — 이 프로젝트에서 가장 중요한 안전장치

**RLS(Row Level Security)** 는 데이터베이스가 **데이터 한 줄 단위로 접근을 막는** 기능입니다.

보통은 이렇게 만듭니다.

> 프로그램이 데이터를 전부 가져온 다음, "이건 이 사람 거니까 보여주고, 저건 남의 거니까 숨기자"

이 방식의 문제는 **프로그램에 실수가 하나라도 있으면 남의 데이터가 새어 나간다**는 것입니다.

RLS는 반대입니다.

> 데이터베이스 자체가 "너는 네 것만 가져갈 수 있어"라고 막습니다.
> 프로그램이 실수로 전부 달라고 해도 **자기 것만 나옵니다.**

도서관에 비유하면, 사서가 일일이 확인하는 게 아니라
**애초에 내 카드로는 내 책장만 열리는** 구조입니다.

학생 데이터를 다루는 서비스에서 이건 매우 중요합니다.
LINK-IN이 새로 만든 테이블에는 **전부 RLS가 걸려 있습니다.**

### 5-3. 서버도 특권을 쓰지 않습니다

이 프로젝트의 눈여겨볼 설계입니다.

Supabase에는 **"모든 제한을 무시하는 마스터 키"**(service_role)가 있습니다.
서버가 이걸 쓰면 편하지만, 그 키가 유출되면 **전체 학생 데이터가 열립니다.**

LINK-IN 서버는 **이 키를 쓰지 않습니다.** 대신 사용자가 로그인할 때 받은
본인의 출입증을 그대로 들고 데이터베이스에 갑니다.
그래서 **서버를 거쳐도 RLS가 똑같이 적용됩니다.**

> 서버가 만능 열쇠를 갖고 있지 않으니, 서버가 뚫려도 피해가 제한됩니다.

### 5-4. 보관하는 데이터

**이번 프로젝트에서 새로 만든 것**

| 이름 | 담는 내용 |
| --- | --- |
| `classes` | 학교·학년·반 정보 |
| `class_memberships` | 누가 어느 반이고 역할(학생/반장/선생님)이 뭔지 |
| `class_notices` | 학급 알림장 |
| `push_subscriptions` | 누구에게 알림을 보낼 수 있는지 |
| `push_jobs` | 예약된 알림 목록과 발송 성공 여부 |

**원래 있던 것** — 회원 정보, 루틴, 반복 루틴, 일별 요약, 보유 아이템

### 5-5. 규칙을 데이터베이스에 새겨 넣기

일부 규칙은 **프로그램이 아니라 데이터베이스가 직접 강제**합니다.
프로그램을 우회해도 뚫리지 않게 하기 위해서입니다.

| 규칙 | 방식 |
| --- | --- |
| 한 반에 반장은 **한 명만** | 데이터베이스가 두 번째 반장 등록을 거부 |
| 학급 변경은 **30일에 한 번** | 데이터베이스 함수 안에서 날짜 검사 |
| 반장 임명은 **같은 반 선생님만** | 함수가 호출자 신분을 먼저 확인 |
| 같은 루틴에 알림 **중복 예약 금지** | 데이터베이스가 중복을 거부 |

또한 데이터베이스 구조를 바꿀 때는 **"추가만 하고 지우지 않는다"** 는 원칙을 지킵니다.
기존 데이터가 실수로 날아가는 일을 막기 위해서입니다.

---

## 6. 외부에 맡긴 일

### 6-1. Google Gemini — 사진 판정 AI

사용자가 "공부하기" 루틴을 인증하려고 사진을 찍으면,
AI가 **사진과 미션이 맞는지 판정**합니다. 책이나 노트가 보이면 높은 점수,
빈 방이나 어두운 화면이면 낮은 점수를 주고, **70점 이상이면 통과**입니다.

**중요한 점: 이 AI를 호출하는 건 서버입니다.**

```
사용자 브라우저  →  우리 서버  →  Gemini
                    (여기에만 열쇠가 있음)
```

AI 서비스를 쓰려면 **유료 열쇠(API 키)** 가 필요한데, 이걸 브라우저에 두면
누구나 열어보고 훔쳐 갈 수 있습니다. 그래서 서버가 대신 물어보고 **결과만** 돌려줍니다.
브라우저는 열쇠의 존재조차 모릅니다.

**남용을 막는 장치**도 넣었습니다. 열쇠 주인이 요금을 내기 때문입니다.

| 장치 | 내용 |
| --- | --- |
| 로그인 확인 | 로그인 안 한 사람은 AI를 못 씀 |
| 횟수 제한 | 한 사람당 **1분에 5번, 1시간에 40번**까지 |
| 사진 크기 제한 | 2MB 넘는 사진 거부 |
| 시간 제한 | 30초 안에 답이 없으면 포기 |
| 사진 축소 | 보내기 전에 **가로세로 768픽셀로 줄임** — 판정엔 충분하고 요금은 절약 |

**AI가 답을 못 주면 어떻게 되나요?**
"판정 실패"로 처리하고 **성공도 실패도 기록하지 않습니다.** 다시 찍으라고만 안내합니다.
예전에는 AI가 실패하면 **무작위로 합격/불합격을 정해서 코인을 줬는데**,
보상이 걸린 판정을 운에 맡기면 안 되므로 제거했습니다.

### 6-2. 웹 푸시 — 앱이 꺼져 있어도 오는 알림

카카오톡 알림처럼, 앱을 안 보고 있어도 화면에 뜨는 알림입니다.

동작 순서는 이렇습니다.

1. 사용자가 "알림 받기"에 동의 → 브라우저가 **전용 주소**를 발급
2. 그 주소를 서버가 저장
3. 알림 예약 시각이 되면 서버가 그 주소로 발송
4. 브라우저 제조사의 우체국을 거쳐 사용자 기기에 도착

이때 **아무나 그 주소로 알림을 보내면 안 되므로**, 서버는 자기만 아는
비밀 열쇠로 서명해서 "진짜 LINK-IN이 보낸 알림"임을 증명합니다.
이 서명을 만드는 부품이 `web-push`입니다.

알림 예약은 데이터베이스에 저장되므로, 서버가 잠깐 꺼졌다 켜져도 사라지지 않습니다.
사용자가 앱을 다시 열면 예약이 복원됩니다.

### 6-3. WebRTC — 화상·음성 통화

학급 친구들과 얼굴을 보며 대화하는 기능입니다.

**특징은 영상이 서버를 거치지 않는다는 것**입니다.
학생 기기끼리 **직접** 연결됩니다. 서버는 처음에 "이 친구랑 연결해 봐" 하고
연락처를 알려주는 역할만 합니다.

- **장점**: 서버 비용이 거의 안 들고, 영상이 서버를 지나가지 않아 사생활에 유리합니다
- **단점 ①**: 인원이 늘면 연결이 급격히 많아집니다. 5명이면 각자 4명과 연결해야 합니다.
  **10명 넘는 학급에는 부적합**합니다
- **단점 ②**: 학교나 회사 방화벽이 엄격하면 **직접 연결이 실패**할 수 있습니다.
  이럴 때 중계해 주는 서버(TURN)가 필요한데 **아직 없습니다**

카메라가 없는 기기에서는 **자동으로 음성 전용으로 전환**됩니다.

---

## 7. 개발할 때 쓰는 명령

명령어는 세 개뿐입니다.

| 명령 | 하는 일 |
| --- | --- |
| `npm start` | 서버 실행 (브라우저에서 `localhost:3000` 접속) |
| `npm run dev` | 실행 + 코드 수정 시 자동 재시작 (개발용) |
| `npm run check` | 코드에 문법 오류가 없는지 검사 |

`npm run check`는 **맞춤법 검사** 수준입니다. 문장 구조가 깨졌는지는 잡아내지만,
내용이 논리적으로 맞는지는 못 봅니다. 빌드 과정이 없어서 이게 유일한 자동 검사입니다.

### 비밀번호와 열쇠 관리

AI 열쇠, 알림 서명 열쇠처럼 **남에게 보이면 안 되는 값**은
`.env`라는 별도 파일에 넣고, 이 파일은 **코드 저장소에 올라가지 않게** 설정되어 있습니다.

여기에 더해 안전장치를 하나 더 넣었습니다.
서버는 **켜지기 직전에 스스로를 검사**해서, 브라우저로 나가는 설정값 안에
비밀 열쇠가 섞여 있으면 **아예 서버를 켜지 않고 멈춥니다.**

> 실수로 열쇠를 노출하는 코드를 넣으면, 인터넷에 배포된 뒤가 아니라
> **내 컴퓨터에서 실행하는 순간 바로 걸립니다.**

### 배포

- **정적 원본 버전** — Vercel이라는 서비스에 올려 두었습니다 (구버전)
- **현재 통합 버전** — 아직 로컬 컴퓨터에서만 실행 중입니다.
  Socket.IO와 알림 예약 때문에 **항상 켜져 있는 서버**가 필요합니다

---

## 8. 왜 이렇게 만들었나 — 선택의 이유

| 선택 | 얻은 것 | 잃은 것 |
| --- | --- | --- |
| 빌드 도구 없음 | 단순함, 빠른 확인, 낮은 진입 장벽 | 자동 오류 검사, 파일 최적화 |
| 부품 3개만 사용 | 보안 위험 감소, 관리 편의 | 직접 만들어야 할 코드가 늘어남 |
| PWA | 앱스토어 불필요, 배포 간편 | 네이티브 앱보다 기기 기능 접근 제한 |
| Supabase | 서버 구축 시간 절약 | 특정 서비스에 의존 |
| RLS 활용 | 데이터 유출 위험 크게 감소 | 초기 설정이 까다로움 |
| 서버가 특권 키 미사용 | 서버가 뚫려도 피해 제한 | 일부 기능(자동 알림 복원) 포기 |
| WebRTC 직접 연결 | 서버 비용 절감, 사생활 보호 | 인원 제한, 방화벽 문제 |

전반적으로 **"단순하고 안전하게, 대신 규모는 작게"** 라는 방향입니다.
학급 단위 프로토타입에는 잘 맞고, 학교 전체로 확대하려면 몇 가지를 바꿔야 합니다.

---

## 9. 지금 구조의 한계

기억할 개념 하나: **"서버 컴퓨터의 메모리에만 있는 정보는 서버가 꺼지면 사라진다."**
현재 세 가지가 여기에 해당합니다.

| 항목 | 무슨 일이 생기나 |
| --- | --- |
| 학급 접속 현황 | 서버를 **여러 대로 늘리면** 같은 반 학생이 서로 다른 교실에 들어감 |
| 알림 예약 타이머 | 서버가 **10분 넘게 꺼져 있으면** 그 사이 알림은 발송 실패로 기록됨 |
| AI 사용 횟수 | 서버 **재시작 시 초기화**되어 횟수 제한이 리셋됨 |

그 외 한계

| 항목 | 내용 |
| --- | --- |
| 화상통화 | 인원이 많으면 부담, 엄격한 방화벽에서 연결 실패 가능 |
| 캐시 버전 | 수정할 때마다 사람이 손으로 올려야 함 (잊으면 반영 안 됨) |
| 외부 라이브러리 | 4곳에 의존, 그중 아이콘은 버전 미고정 |
| 서버리스 이전 | 지금 구조로는 불가능 — 위 세 가지가 전부 깨짐 |

이 항목들의 상세 내용과 개선 후보는 [`AUDIT.md`](AUDIT.md)에 정리되어 있습니다.

---

---

# 10. 부록 — 개발자용 정확한 정보

여기부터는 정확한 버전과 목록입니다.

## 10-1. 요약표

| 계층 | 채택 기술 |
| --- | --- |
| 런타임 | Node.js 22 (LTS) |
| 웹 서버 | Express 4 |
| 실시간 통신 | Socket.IO 4 (WebSocket / 폴백 폴링) |
| 화상·음성 | WebRTC (P2P mesh, STUN) |
| 프런트엔드 | 빌드 도구 없는 바닐라 JS (ES2020+) |
| 렌더링 | DOM 직접 조작 + Canvas 2D |
| 인증 | Supabase Auth (JWT) |
| 데이터베이스 | Supabase PostgreSQL + RLS |
| DB 접근 | PostgREST (서버) / supabase-js v2 (브라우저) |
| 푸시 알림 | Web Push (VAPID) + Service Worker |
| AI | Google Gemini `gemini-3.6-flash` (서버 프록시 경유) |
| 앱 형태 | PWA (manifest + Service Worker) |
| 배포 | Vercel (정적 원본) / 통합본은 로컬 Node 실행 |

## 10-2. 서버 의존성

| 패키지 | package.json | 실제 설치 | 역할 |
| --- | --- | --- | --- |
| Node.js | — | **22.23.2** | 런타임 |
| npm | — | 10.9.8 | 패키지 관리 |
| `express` | `^4.19.2` | 4.22.2 | HTTP 서버, 정적 서빙, REST |
| `socket.io` | `^4.7.5` | 4.8.3 | 실시간 양방향 통신 |
| `web-push` | `3.6.7` (고정) | 3.6.7 | VAPID 서명 및 푸시 발송 |
| `nodemon` | `^3.1.0` (dev) | — | 개발 중 자동 재시작 |

Node 22 내장 기능으로 대체한 것: `--env-file`(dotenv), 전역 `fetch`(axios),
`AbortSignal.timeout()`, `node --check`(정적 검사).

## 10-3. HTTP 엔드포인트

| 메서드 | 경로 | 인증 | 설명 |
| --- | --- | --- | --- |
| GET | `/runtime-config.js` | 없음 | 공개 설정 동적 생성 (`no-store`) |
| POST | `/api/push/subscribe` | Bearer | 푸시 구독 저장 + 대기 예약 복원 |
| POST | `/api/push/schedule` | Bearer | 루틴 알람 예약 (최대 7일) |
| POST | `/api/ai/analyze` | Bearer | Gemini 사진 판독 프록시 |
| GET | `/*` | 없음 | `public/` 정적 파일 |

## 10-4. Socket.IO 이벤트

| 이벤트 | 방향 | 설명 |
| --- | --- | --- |
| `join-class` | C→S | 학급 입장 (역할·학급은 서버가 DB 조회로 결정) |
| `joined-success` / `join-error` | S→C | 입장 결과 |
| `user-joined` / `user-left` | S→C | 구성원 변동 |
| `pixel-move` / `pixel-user-moved` | C→S→C | 좌표 동기화 (서버 clamp) |
| `update-notice` / `notice-updated` / `notice-error` | 양방향 | 알림장 |
| `webrtc-offer` / `-answer` / `-ice` | C→S→C | 시그널링 릴레이 (동일 룸만) |

룸 ID는 `class_id`(UUID). 룸 상태는 서버 메모리(`roomsData`).

## 10-5. 프런트엔드 로드 순서

```html
<script src="https://unpkg.com/lucide@latest"></script>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
<script src="runtime-config.js?v=2"></script>       <!-- 서버 동적 생성 -->
<script src="store.js?v=28"></script>
<script src="ai.js?v=31"></script>
<script src="avatar.js?v=28"></script>
<script src="app.js?v=30"></script>
<script src="pixelClassroom.js?v=3"></script>
<script src="online.js?v=7"></script>
```

스타일: `style.css?v=29`, `online.css?v=4`

## 10-6. 모듈과 전역

| 파일 | 크기 | 노출 전역 |
| --- | --- | --- |
| `app.js` | ~100KB | `window.lockinSupabase` + 최상위 함수 다수 |
| `store.js` | ~30KB | `window.godsaengStore` |
| `avatar.js` | ~22KB | `window.avatarRenderer` |
| `ai.js` | ~8KB | `window.visionAI` |
| `online.js` | ~22KB | `window.scheduleServerPushForTask` |
| `pixelClassroom.js` | ~12KB | `window.PixelClassroom` |

`app.js`는 클래식 스크립트라 최상위 `function` 선언이 자동 전역이 됩니다
(`online.js`가 `window.openAuthModal`을 호출하는 것이 그 예). 이름 충돌에 주의.

## 10-7. 사용 브라우저 API

| API | 사용처 |
| --- | --- |
| Canvas 2D | `avatar.js` `pixelClassroom.js` `app.js` `ai.js` |
| `getUserMedia` | `app.js` `online.js` |
| `RTCPeerConnection` | `online.js` |
| Service Worker | `sw.js` |
| `PushManager` | `online.js` |
| Notification | `app.js` `sw.js` |
| `localStorage` | `store.js` `app.js` `ai.js` |
| `canvas.toDataURL` | `app.js` `ai.js` |
| `navigator.vibrate` | `app.js` |

## 10-8. Supabase 스키마

**신규 테이블** (모두 RLS 적용)

| 테이블 | 설명 |
| --- | --- |
| `classes` | 학교·학년·반 (3개 조합 unique) |
| `class_memberships` | 사용자당 1행. `student`/`teacher`/`president` |
| `class_notices` | 학급 알림장 |
| `push_subscriptions` | 푸시 구독 (endpoint unique) |
| `push_jobs` | 예약 큐 (`pending`/`sent`/`failed`/`cancelled`) |

**기존 테이블** — `profiles`, `tasks`, `routine_templates`, `daily_summaries`, `user_items`

`tasks`·`routine_templates`에 `alarm jsonb` 컬럼 추가
(`{"enabled": bool, "time_24h": "HH:MM"}` 형태 check 제약).

**RPC 함수** — 모두 `security definer` + `set search_path = ''`, `anon` 실행 권한 회수

| 함수 | 역할 |
| --- | --- |
| `join_class(school, grade, class_no, name)` | 학급 생성·가입, 30일 1회 변경 제한 |
| `set_class_president(user_id, class_id)` | 반장 임명, 호출자 교사 검증 |
| `upsert_class_notice(title, content)` | 알림장 저장, 권한 검증 |

**무결성 장치** — `one_president_per_class` partial unique index,
`push_jobs (user_id, task_id)` unique, 모든 마이그레이션은 추가 전용.

## 10-9. Gemini 호출 사양

| 항목 | 값 |
| --- | --- |
| 모델 | `gemini-3.6-flash` |
| 엔드포인트 | `POST /v1beta/models/{model}:generateContent` |
| 인증 | `x-goog-api-key` 헤더 (신형 `AQ.` 키는 `?key=` 미지원) |
| 출력 | `responseMimeType: application/json` |
| 호출 위치 | 서버 전용 (`/api/ai/analyze`) |
| 방어 | 로그인 검증 → 형식 검증 → 5회/분·40회/시간 → 30초 타임아웃 |
| 전처리 | 클라이언트에서 긴 변 768px, 품질 0.8로 축소 |

> `gemini-2.5-flash`는 모델 목록에는 보이지만 신규 사용자에게 404를 반환합니다.
> 진단: `node --env-file=.env diagnose-gemini.js`

## 10-10. WebRTC

P2P full mesh. ICE 서버는 `stun:stun.l.google.com:19302` 하나. TURN 없음.

## 10-11. 보존본이 쓰는 기술 (미실행)

`login/`과 `Link-In-온라인 시스템/src/`의 샘플입니다. 빌드되지 않으며 앱에 포함되지 않습니다.

- TypeScript (`.ts` / `.tsx`)
- React 함수형 컴포넌트 (`LoginForm.tsx`, `TeacherDashboard.tsx`)
- `@supabase/supabase-js` npm import 방식

두 폴더의 파일 4개는 바이트 단위로 동일합니다. `package.json`에 React·TypeScript·번들러
의존성이 없으므로 되살리려면 빌드 파이프라인부터 구축해야 합니다.

## 10-12. 환경변수와 캐시

`.env`는 `.gitignore` 대상. 전체 목록은 [`README.md`](README.md).

- **브라우저로 내려가는 값**: Supabase URL, anon 키, VAPID 공개키, `AI_PROXY_ENABLED`, 모델명
- **서버에만 머무는 값**: `GEMINI_API_KEY`, `VAPID_PRIVATE_KEY`, (미사용) `SUPABASE_SERVICE_ROLE_KEY`

서버는 기동 직전 `assertNoSecretLeak()`으로 `/runtime-config.js` 응답에 비밀값이
섞이지 않았는지 검사하고, 발견되면 포트를 열지 않습니다.

**캐시 무효화 — 정적 자산 수정 시 두 곳을 함께 올릴 것**

1. `public/index.html`의 `?v=` 쿼리
2. `public/sw.js`의 `CACHE_NAME` (현재 `link-in-integrated-v13`)

Service Worker가 cache-first라 버전을 올리지 않으면 수정본이 반영되지 않습니다.
단, 내비게이션 요청과 `/runtime-config.js`는 항상 네트워크 우선입니다.

## 10-13. 구조적 제약 요약

| 항목 | 현재 | 영향 |
| --- | --- | --- |
| 룸 상태 | 프로세스 메모리 | 수평 확장 시 룸 분리 (Redis adapter 필요) |
| 푸시 타이머 | 프로세스 메모리 | 재시작 시 유예 10분 초과분 유실 |
| AI 호출 한도 | 프로세스 메모리 | 재시작 시 초기화, 인스턴스별 집계 |
| WebRTC | STUN only, full mesh | 엄격한 NAT 실패 가능, 대규모 부적합 |
| 빌드 | 없음 | 배포 단순, 타입 검사·번들 최적화 부재 |
| 캐시 무효화 | 수동 버전 | 누락 시 미반영 |
| CDN | 4곳 의존, 1개 `@latest` | 오프라인 최초 실행 불가, 재현성 결함 |
